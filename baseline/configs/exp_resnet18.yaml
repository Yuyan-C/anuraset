# Here's where you define experiment-specific hyperparameters.
# You can also create lists and group parameters together into nested sub-parts.
# In Python, this is all read as a dict.

# environment/computational parameters
seed: 32678456782       # random number generator seed (long integer value)
device: cuda
num_workers: 4

# dataset parameters
data_root: /network/scratch/y/yuyan.chen/anuraset/
metadata: INCT20955_metadata.csv
num_classes: 12
folder_name: resnet18_INCT20955
# training hyperparameters
image_size: [224, 448] #[128, 512] 
num_epochs: 1
batch_size: 32
learning_rate: 0.01
weight_decay: 0.001
model_type: resnet18
pretrained: true
multilabel: true
wandb_project: anuraset
wandb_entity: mila-org-test
wandb_name: resnet18_INCT20955_baseline

